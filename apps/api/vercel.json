{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bash -c 'if [ -f ../../pnpm-lock.yaml ]; then cd ../.. && corepack enable && pnpm install --no-frozen-lockfile; else corepack enable && pnpm install --no-frozen-lockfile; fi'",
  "buildCommand": "node scripts/vercel-build.mjs"
}
